---
title: "Cilium Service Mesh & Gateway API"
date: "2024-03"
tags: ["Cilium", "Kubernetes", "eBPF", "Networking", "Security"]
summary: "Implementing Cilium as the CNI and service mesh layer for a multi-tenant Kubernetes platform, with Gateway API for advanced traffic management and identity-based network policy."
status: "shipped"
---

## Why Cilium

Default Kubernetes network policy works at the IP/port level, which falls apart when pods are short-lived and IPs get reused. **Cilium** uses **eBPF** to enforce policy in the kernel based on workload identity instead of IP addresses.

On a multi-tenant platform that's the difference that counts. Policies follow what a workload *is*, so they keep holding as pods restart and move around rather than going stale the moment an IP changes.

## Gateway API Implementation

Implemented the **Cilium Gateway API** (certified, as evidenced by the Cilium Gateway API and Advanced Use Cases certifications) as the ingress and traffic management layer. The Gateway API is a more expressive successor to the Ingress resource — it supports traffic splitting, header-based routing, and cross-namespace references that make it genuinely useful for platform-level traffic management.

The advanced use cases certification covered scenarios including cross-namespace routing, TLS termination, and traffic mirroring for canary deployments.

## Network Policy Architecture

Designed the layered network policy model for the platform:
- **Cluster-wide policies**: managed by the platform team, not overrideable by tenants (egress to the internet, inter-namespace defaults)
- **Tenant policies**: scoped to a team's namespace, giving them control over their own service-to-service communication
- **Default-deny baseline**: all traffic denied unless explicitly permitted, enforced via a base policy that applies to every namespace at provision time

So the platform denies by default and teams opt in to the paths they actually need, instead of trying to remember to close the ones they don't.

## eBPF Observability

A nice side benefit of the eBPF data plane is that you get detailed network flow visibility almost for free. I used Hubble (Cilium's observability layer) to give teams a live view of which services were talking to which, where policy was dropping connections, and what latency looked like down at the network layer.
